package string;

import java.util.Scanner;

public class string_basic {
    
    public static void main(String arg[]){
        Scanner sc=new Scanner(System.in);
        // input in string
        String name;
        name=sc.nextLine();
        name=sc.next();

        // length of string 
        name.length();//gives length of string name
                        // it is fuction

        // Array.length;   
        // gives aaray length 
        // its a property of an array

        // concatination of string= adding two strings

        name.charAt(0);
        // gives char at index 0th locarion
    }
}
